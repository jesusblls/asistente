node_modules/
dist/
build/
.next/
next-env.d.ts
.env
.env.local
.env.*.local
# Bare ".env" solo cubre ese nombre exacto, no "deploy/.env.production" (el
# real, con secretos — deploy/.env.production.example sí se sube, es la
# plantilla sin valores). Sin esta línea, seguir deploy/README.md al pie de
# la letra localmente ("cp deploy/.env.production.example
# deploy/.env.production") deja el archivo con secretos reales sin ignorar.
.env.production
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
coverage/
.turbo/
*.sqlite
*.sqlite-journal
*.db
*.db-journal
*.tsbuildinfo
.chrome-user-dir/
# Enlace simbólico a las skills locales del desarrollador (fuera del repo)
.agents/
apps/api/src/**/*.js
apps/api/src/**/*.js.map
apps/api/src/**/*.d.ts
test-results/
playwright-report/
blob-report/
.playwright/
.gstack/
# Capturas de revisión de diseño (se regeneran en cada verificación)
**/.impeccable/review/
.claude/
