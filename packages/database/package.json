{
  "name": "@asistente/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "seed": "tsx src/seed.ts",
    "admin:create": "tsx src/create-admin.ts",
    "credentials:rotate": "tsx src/rotate-credentials.ts",
    "migrate": "prisma migrate deploy",
    "migrate:status": "prisma migrate status",
    "build": "prisma generate && tsc",
    "dev": "tsc -w"
  },
  "dependencies": {
    "@asistente/observability": "*",
    "@asistente/shared-types": "*",
    "@prisma/client": "^6.4.1",
    "ioredis": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.4.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
